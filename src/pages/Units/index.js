import React from 'react';
import Page from '../../layout/Page';

const Units = (props) => {
    return (
        <Page>
            <div>
                Units
            </div>
        </Page>
    );
};

export default Units;