import React from 'react'
import Page from '../../layout/Page';

const Convert = (props) => {
    return (
        <Page>
            <div>
                Convert
            </div>
        </Page>
    );
};

export default Convert;